/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.1.3 public/skeleton.gltf
*/

import React from 'react';
import { ModelImport } from './modelParts/SkeletonNecron.jsx';
import { ModelObject } from './modelParts/ModelObject.jsx';
export function Necron(props) {
  const {
    currentPaint,
    armR,
    arm,
    armRRot,
    paintRef,
    show,
    squadIndex,
    baseColor,
    clone,
  } = props;

  function modelFactory(url) {
    return (
      <ModelObject
        currentPaint={currentPaint}
        paintRef={paintRef}
        show={show ? 1 : 0}
        squadIndex={squadIndex}
        url={url}
        baseColor={baseColor}
        clone={clone}
      />
    );
  }

  return (
    <group>
      <ModelImport
        torsoBone={props.torsoBone}
        neck={props.neck}
        torso={modelFactory('necron_torso.glb')}
        armR={
          armR === 'reaper'
            ? modelFactory('necron_reaper.glb')
            : modelFactory('necron_flayer.glb')
        }
        armRRot={armRRot}
        base={
          <ModelObject
            currentPaint={currentPaint}
            paintRef={paintRef}
            show={show ? 1 : 0}
            squadIndex={squadIndex}
            url="base_medium.glb"
            baseColor={baseColor}
            clone={clone}
          />
        }
        currentPaint={props.currentPaint}
        torsoTopBone={props.torsoTopBone}
        paintRef={paintRef}
        baseColor={baseColor}
      />
    </group>
  );
}
